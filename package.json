{
  "name": "shop-web",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-config.js",
    "check": "find api scripts -name '*.js' -print0 | xargs -0 -n1 node --check && node -e \"JSON.parse(require('fs').readFileSync('vercel.json','utf8')); console.log('vercel.json OK')\""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0",
    "resend": "^6.0.0",
    "twilio": "^5.4.0",
    "web-push": "^3.6.7"
  }
}
